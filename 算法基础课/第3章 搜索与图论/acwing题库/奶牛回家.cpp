#include <iostream>
#include <algorithm>
#include <cstring>
#include <vector>

using namespace std;

const int N = 60;

int g[N][N];
int dist[N];
bool st[N];
int n, m;

int main()
{
    cin>>m;
    
}